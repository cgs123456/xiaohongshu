spring:
  config:
    activate:
      on-profile: local
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    url: jdbc:mysql://localhost:3306/rb_note?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456chen
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: rabbitmq123
  kafka:
    bootstrap-servers: localhost:9092
  redis:
    host: localhost
    port: 6379
    password: redis123
  data:
    mongodb:
      host: localhost
      port: 27017
      username: root
      password: mongo123
  elasticsearch:
    uris: http://localhost:9200
